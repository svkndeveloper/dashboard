import { StyledLi } from "./Card.styled";

export const Card = ({ card }) => {
    // const {  title, difficulty, status, time, dat, category, type } = card;
    return (
        <StyledLi>

        </StyledLi>
    )
}